我收集了以下问题：
1. print是函数，许多同学忘记加上（），同时end, sep等概念依旧不熟。
2. string的初始化，经常有同学直接缺失双引号，如a=Hello Python。还有同学会把str等内置key当做变量，导致一些问题
3. 面向对象的概念依旧不适应，比如字符串的一些函数，他们依旧习惯直接写函数名，然后把字符串变量放括号里，如lower(a)
4. 同样的，有些函数是python内置的，如len, abs等，他们会和字符串对象的内置函数搞混。